# คำสั่ง
- เมื่อทำ task ใดเสร็จแล้ว ให้ [x] ด้วย

# task-01
- [x] จะมีให้แก้ไข uxui ตาม mockup ใน folder \materials\mockup
- [x] ตอนนี้ยังไม่มีหน้าที่เป็นหน้าข้อมูลนักเรียนอันหลักเลย ช่วยสร้างให้หน่อย โดยอิงรูปร่างหน้าตาจาก `index.html`
- [x] ช่วยดูให้หน่อยว่า ควรสร้างหน้าอะไรเป็นหน้าหลักบ้าง ถ้ามีในใจแล้ว สร้างเพิ่มขึ้นมาได้เลย

# task-02
- [x] แก้ index.html ตาม 00_admission ได้เลย
- [x] สร้างหน้า form รับสมัครตาม 01_apply ได้เลย
- [x] หน้ารับสมัครและคัดเลือกทำไมมี column สถานะการชำระเงินซ้ำกัน เอาสถานะค่าสมัครออก ให้เหลือแค่สถานะหลักก็ได้ แก้ไขให้หน่อย แบบที่นายไม่ต้องรัน test นะ เดี๋ยวฉันรันเอง

# task-03-2026-07-13
- [x] ตอนนี้ยังไม่มีหน้าข้อมูลนักเรียนที่เป็นหน้าหลัก สร้างให้หน่อย โดยอ้างอิงรูปร่างหน้าตาจาก html file อื่น ๆ ได้เลย โดยอิงจากไฟล์ ข้อมูลนักเรียน.png
- [x] ต่อมา ทำหน้า settings โดยอิงจากภาพใน folder settings ทำให้มีข้อมูลเหมือนในรูปเป๊ะ ๆ เลย ที่มีพวก switch ตั้งค่า และมี tab settings ต่าง ๆ
    - [x] เมื่อสร้างเสร็จแล้ว ให้แบ่งกลุ่มของหัวข้อ switch ของแต่ละ tab โดย ให้ตีกรอบหรือระบายสีเป็นโซน คล้าย ๆ การตั้งค่าใน iphone ที่จะ แบ่งเป็นเมนูของแต่ละหัวข้อ จากนั้นเข้ามาก็จะเห็นการแบ่งกลุ่มของ switch อีกทีด้วย
- [x] ในหน้า room_exam_settings.html มันจะมี popup ที่ขึ้นมาเมื่อกดเพิ่มหรือแก้ไขห้องสอง ให้ใช้วิธี กรอกจำนวนนักเรียนทั้งหมดในแผนที่จะสอบ (จริง ๆ คืออยากอิงจำนวนมาจากของหน้า admission) แล้วบรรทัดต่อมาก็เป็นว่า ขึ้นว่าห้องเรียนที่ต้องใช้ มีกี่ห้อง มีเลขห้องอะไรบ้าง ทำเป็น mockup ให้เห็นภาพเฉย ๆ ยังไม่ต้องคำนวณขนาดนั้น
- [x] ให้เปลี่ยน radio option from ใช้ตามจำนวนในแผนการเรียน to ใช้ตามขนาดห้องเรียน
- [x] อยากให้มีฟันเฟืองอยู่หลังตอนที่เราเลือก radio option ใช้ตามขนาดห้องเรียน แล้วพอกดพาไปหน้าแผนการเรียน แต่ตอนนี้เป็น mockup เราจะมีแค่ popup ว่าพาไปหน้านั้นแล้วเฉยๆพอ
- [] ทำให้ทุกห้องมี suggest แบบห้องวิทย์คณิตได้ไหม ที่มีหลายตัวเลือก แต่ตอนนี้งงว่าเขาเอาห้องประชุมหายไปไหนแล้วด้วย ของวิทย์คณิตอ่ะ
- [] ตอนนี้รู้สึกหน้าตั้งค่ามันทะแม่ง ๆ ยังไงไม่รู้ หน้ตั้งค่าฟิลรับสมัครกับแผนหลักสูตร ตั้งค่าห้องสอบ

# task-04-2026-09-05
- [x] Flow 1: การส่งข้อมูล REGIS สช. — เพิ่มเมนู "ส่งข้อมูล REGIS สช." ใน dropdown นำเข้า/ส่งออก ของหน้า students.html แล้วแยกเป็น 2 ทางเลือก
    - [x] ทางเลือก A (เฟสเตรียมเชื่อมต่อ API): Login เข้า REGIS → popup แจ้งว่าจะพาไปหน้า Login ของ REGIS สช. + ปุ่ม "ไปยังระบบ REGIS"
    - [x] ทางเลือก B (เฟสแรก): Export as Excel → ผู้ใช้ได้ไฟล์ไปอัปโหลดใน REGIS สช. เอง (ตอนนี้ mock เป็น toast ก่อน)
- [x] Flow 2: การเลื่อนชั้น / จบการศึกษา — เพิ่ม step ที่ขาดในโมดัลเลื่อนชั้นเดิม
    - [x] ถ้าไม่ได้ติ๊กใครออกเลย → ข้ามไป popup ยืนยันทันที
    - [x] ถ้าติ๊กคนที่ไม่ผ่านออก → แสดงหน้าสรุปรายชื่อคนที่ไม่ได้ไปต่อก่อน (มีปุ่มย้อนกลับ) แล้วค่อยไป popup ยืนยัน
    - [x] ปุ่มสุดท้ายชื่อ "เลื่อนชั้น" เพื่อเสร็จสิ้นกระบวนการ
- [x] Flow 3: การย้ายห้องเรียน — เพิ่มเมนู "ย้ายห้องเรียน" ใน dropdown จัดการทะเบียน
    - [x] popup เลือกห้องปลายทาง โดยค่าเริ่มต้นเป็นการย้ายยกห้อง ไม่ต้องติ๊กเลือกรายบุคคล (ห้องต้นทางถูกเติมให้อัตโนมัติจากตัวกรอง/คนที่เลือกไว้)
    - [x] popup ยืนยัน แล้วจึงย้ายห้องเรียนสำเร็จ
    - [x] เพิ่มฟิลด์ `classroom` ที่บันทึกจริงลง localStorage (เดิมห้องเรียนถูกคำนวณจากแผนการเรียน จึงย้ายไม่ได้) พร้อม `effectiveClassroom()` ให้ทุกหน้าแสดงห้องตรงกัน

# task-05-2026-09-06
- [x] จำลองนักเรียนให้ครบทุกห้อง (ห้องละ 3-4 คน) — เดิมมี enrolled แค่ 9 คน กระจาย 9 ห้อง 21 ห้องว่าง เพิ่ม generator แบบ deterministic ใน store.js เติมจนครบทั้ง 30 ห้อง (6 ระดับชั้น × 5 ห้อง) รวมเป็น ~108 คน
- [x] ขยายโมดัลเลื่อนชั้น/จบการศึกษาให้ใหญ่ขึ้น ไม่ต้องประหยัดพื้นที่ — ปรับเป็น 2 คอลัมน์ (สรุปซ้าย / รายชื่อขวา), การ์ดกว้างขึ้น (`max-w-5xl`), รายชื่อเลื่อนดูง่ายขึ้น (`max-h-[55vh]`), เพิ่ม `setMockModalSize()` ให้แต่ละโมดัลกำหนดความกว้างเองได้โดยไม่ค้างข้ามโมดัล
- [x] ยุบเมนู "ย้ายเข้าจากสถานศึกษาอื่น (Transfer In)" รวมเข้ากับเมนูใหม่ "เพิ่มนักเรียน" (ปุ่มเดี่ยวในแถบหัวตาราง) — เลือกโหมด "นักเรียนใหม่" หรือ "ย้ายเข้าจากสถานศึกษาอื่น" ในโมดัลเดียว บันทึกเข้าตารางตรงๆ ไม่ผ่านระบบ admission

# task-06-2026-09-07
- [x] Phase 1 — เปลี่ยนธีมจากมืดเป็นสีขาวทั้งระบบ: `students.html`, `settings.html`, `course_settings.html`, `room_exam_settings.html` (inline `<style>`, header, utility class ทั้งหมด, dropdown/modal overlay, ID card mockup คงมืดไว้ตั้งใจ) — เหลือแค่ QR PromptPay ใน `status.html` ที่ตั้งใจให้มืดตามเดิม
- [x] Phase 2 — โมดัลเลื่อนชั้น: sidebar ระดับชั้นเป็นตัวกรอง (ซ่อนแถว count=0), chip ห้องเรียนของชั้นที่เลือก (`window.classroomOptions()`, รองรับ 16+ ห้อง), แก้บั๊ก `renderPromotionCandidatesListOnly()`/`selectClassroom()` ที่เทียบด้วย `innerText` ให้ใช้ `data-grade`/`data-room` แทน
- [x] Phase 3 — สรุปการเลื่อนชั้นเป็น KPI คู่ (เลื่อนชั้น/จบ ↔ ยกเว้นไว้) มี caption + icon กำกับ, เลข 0 เป็นสีเทาไม่ใช่แดง, คลิกไทล์แดงเพื่อกรองรายชื่อเฉพาะคนที่ถูกยกเว้น (แทนปุ่ม "ล้างการยกเว้นทั้งหมด" เดิม)
- [x] Phase 4 — เพิ่มปุ่ม "ย้ายห้องเรียน" ใน bulk action bar, เพิ่ม segmented control เลือกโหมด "ย้ายยกห้อง" / "เฉพาะที่เลือก" ในโมดัล, เปลี่ยนรายชื่อจาก `<details>` เป็น panel เปิดค้างพร้อม checkbox ต่อคน + ปุ่มเลือกทั้งหมด/ล้าง
- [x] Phase 5 — เพิ่ม dropdown "เรียงตาม" (ชื่อ/นามสกุล/รหัสนักเรียน/เลขที่/ชั้นเรียน/วันที่เข้าเรียน/สถานะ/ค่าเริ่มต้น) พร้อมปุ่มสลับทิศทาง เรียงห้องเรียนแบบตัวเลขจริง (ไม่ใช่ string sort)
- [x] Phase 6 — เพิ่มตัวเลือกจำนวนต่อหน้า (10/25/50/100/200, ค่าเริ่มต้นจาก 8 เป็น 25), ปุ่มเลขหน้าแบบ windowed ไม่ล้นแถวเมื่อมีหลายสิบหน้า, clamp หน้าปัจจุบันเมื่อเปลี่ยนขนาดหน้า/ตัวกรอง, จำค่า pageSize+sort ไว้ใน `localStorage['admitify_students_prefs']`

# task-07-2026-09-07
- [x] แก้บั๊กโมดัลย้ายห้องเรียน: เลือกนักเรียนข้ามห้อง (เช่น 1/1 กับ 1/3) แล้วห้องต้นทางเดิมโชว์แค่ห้องเดียว — เพิ่ม `getMoveClassroomSourceRooms()` รองรับหลายห้องต้นทางพร้อมกัน อัปเดต chip แสดงห้อง, การกรองห้องปลายทาง, ข้อความยืนยัน/log/toast ให้ join ครบทุกห้อง
- [x] ปุ่ม "ล้าง" ในลิสต์รายชื่อของโมดัลย้ายห้องเรียนเปลี่ยนเป็นสีแดง (`text-rose-600`) ให้ต่างจาก "เลือกทั้งหมด" (สีฟ้า) — ใช้สไตล์เดียวกันนี้กับโมดัลย้ายออกและ ปพ.2 ด้วย
- [x] โมดัลย้ายออก/ลาออก เปลี่ยนจาก chip preview อ่านอย่างเดียว เป็นรายชื่อ+checkbox ต่อคน พร้อมปุ่มเลือกทั้งหมด/ล้าง แบบเดียวกับย้ายห้องเรียน (แยก container ย่อยเพื่อไม่ล้างค่าที่กรอกในฟอร์มประเภท/โรงเรียนปลายทาง/เหตุผลเมื่อติ๊ก)
- [x] บังคับให้ต้องติ๊กเลือกนักเรียนอย่างน้อย 1 คนก่อนถึงจะเปิดโมดัลย้ายออก/ลาออกได้ (ตัดพฤติกรรม default ไปทั้งลิสต์ที่กรองอยู่เมื่อไม่ได้ติ๊กใครออก) ขึ้น toast เตือนแทน
- [x] เพิ่มปุ่ม "ออกเอกสาร" (รวม ปพ.1/ปพ.2 เป็นเมนูย่อยเดียว) ใน floating bulk-action bar โดยเพิ่ม `dd-bulk-docs` เข้า `DROPDOWN_IDS` ให้ใช้ระบบ toggle/ปิดอัตโนมัติเดิม
- [x] โมดัลออกเอกสาร ปพ.2 เพิ่มการเลือกเป็นรายบุคคล — รายชื่อ+checkbox แบบเดียวกับย้ายห้องเรียน รองรับทั้งจากนักเรียนที่เลือกไว้ในตาราง หรือกรองตามระดับชั้นเมื่อไม่ได้เลือกใคร (เดิมออกเป็นกลุ่มล้วนๆ ไม่รับรู้ selection เลย)
# task-08-2026-09-07
- [x] โมดัลนำเข้าข้อมูลนักเรียน: เพิ่มปุ่ม "ดาวน์โหลด Template (.csv)" ที่โหลดไฟล์จริงด้วย `Blob` (ไฟล์แรกของ repo ที่ดาวน์โหลดได้จริง ไม่ใช่ mock) — นำหน้าด้วย UTF-8 BOM ให้ Excel อ่านภาษาไทยไม่เพี้ยน มี header 13 คอลัมน์ + แถวตัวอย่าง 2 แถว, เพิ่มเมนู "ดาวน์โหลด Template นำเข้า" ใน dropdown นำเข้า/ส่งออก ด้วย
- [x] แยกนักเรียนปัจจุบัน/ศิษย์เก่าด้วย **ไฟล์เดียว + คอลัมน์ "ประเภท" + segmented control เลือกค่าเริ่มต้นตอนอัปโหลด** (ใช้เฉพาะแถวที่เว้นคอลัมน์ประเภทว่างไว้) — ศิษย์เก่า map ไปที่สถานะ `graduated`, ปัจจุบัน = `enrolled`; เพิ่ม `<details>` แสดงคอลัมน์ที่รองรับ และเปลี่ยน drop zone จาก `<div>` ตายๆ เป็น `<input type="file">` จริง (คลิก/ลากวางได้, ปุ่ม "เริ่มนำเข้า" disabled จนกว่าจะเลือกไฟล์, log/toast อ้างชื่อไฟล์+ประเภทจริง)
- [x] navbar หน้าข้อมูลนักเรียน: ลบ ONLINE pill / ตรวจสอบสถานะ / ลงทะเบียนนักเรียนใหม่ ออก (ปล่อยฝั่งขวาว่าง) — แก้เฉพาะ `students.html` ตามที่ผู้ใช้ระบุ, `settings.html`/`course_settings.html`/`room_exam_settings.html` ยังมีบล็อกเดิมอยู่
- [x] ตัวกรองสถานะ default เปลี่ยนจาก "ทั้งหมด" เป็น "กำลังศึกษา" (`activeStatus = 'enrolled'` + ย้าย class `active` + `clearAllFilters()` รีเซ็ตกลับมาที่ enrolled)
- [x] กด chip ระดับชั้นหรือห้องเรียน → สลับ dropdown "เรียงตาม" เป็น **เลขที่ในห้อง น้อยไปมาก (1→N)** อัตโนมัติ พร้อม pill "อัตโนมัติ" กำกับ; แยก `userSort` ออกจาก `activeSort` ให้กลับไปดูทั้งโรงเรียนแล้วคืนค่าที่ผู้ใช้ตั้งเอง และไม่บันทึกค่าที่ระบบเซ็ตให้ลง prefs (`applySortSilently()`, `syncSortForScope()`)
- [x] แก้ comparator `seat` ที่ลบสตริงกันตรงๆ และไม่มี secondary key — เปลี่ยนเป็นจัดกลุ่มตามห้องก่อนแล้วค่อยเรียงเลขที่ (`parseInt`) ไม่งั้นดูทั้งระดับชั้นแล้วเลขที่ 1 ของทุกห้องปนกัน
- [x] chip ห้องเรียนเลิกฮาร์ดโค้ด เปลี่ยนมาใช้ `window.classroomOptions()` ตัวเดียวกับโมดัลอื่น (จาก 12 ห้องมั่วๆ → ครบ 30 ห้อง พร้อมจำนวนคนบน chip)
- [x] แถบ bulk action ที่ลอยอยู่: badge นับจำนวนจาก `h-6 w-6` วงกลมตายตัว (เลข 3 หลักล้น) → `min-w-[1.5rem] w-auto px-1.5`, เพิ่ม `flex-wrap` + `max-w-[calc(100vw-2rem)]`, ขยาย gap/padding ของแถบและปุ่มทุกอัน
- [x] เพิ่มโมดัลแจ้งเตือนแบบบล็อก `#modal-notice` + helper `openNotice()` (ไม่มีปุ่ม X, ปิดได้ทางเดียวคือกด "รับทราบ") แล้วเปลี่ยน toast "เลือกข้ามระดับชั้น" ของโมดัลย้ายห้องเรียนมาใช้ตัวนี้ — สรุปว่าเลือกชั้นไหนมากี่คน + ปุ่มลัด "ไปที่เลื่อนชั้นเรียน" (กฎย้ายได้เฉพาะในชั้นเดียวกันยังคงเดิม, เลือกข้ามห้องในชั้นเดียวกันยังทำได้ตามปกติ)
- [x] แก้ label โหมด "ย้ายยกห้อง" ให้ระบุชื่อห้อง (`ย้ายยกห้อง ม.1/1 (4 คน)`) กันเข้าใจผิดว่าเป็นยอดรวมทุกห้องที่เลือก

# task-09-2026-09-07
- [x] เพิ่มระบบ scope กลาง (`ACTION_SCOPES` + `getActionScope()` + `refreshActionLabels()`) ให้ทุกแถวเมนู/ปุ่ม action เขียนป้ายบอกขอบเขตของตัวเองใหม่ตามสถานะจริง (`— N คนที่เลือก` / `— N รายการตามตัวกรอง` / `— ทั้งโรงเรียน (N คน)` / `— ยกห้อง ม.1/1 (N คน)` / `— ต้องติ๊กเลือกก่อน` พร้อม disable ปุ่ม) แทนป้าย `.dd-selection-label` เดิมที่เขียนข้อความเดียวกันทับทุก panel ทั้งที่แต่ละแถวมี scope ไม่เหมือนกันจริง (ที่แย่สุดคือ `#dd-reports` ที่ขึ้น badge/ป้ายทั้งที่ทั้ง 2 เมนูไม่เกี่ยวกับ selection เลย — ตัดออก)
- [x] ปุ่มที่มี 2 ทางเข้า (dropdown หัวตาราง vs `#bulk-action-bar`) ให้แปลคนละความหมายถาวรโดยส่ง scope เป็นพารามิเตอร์ตรงๆ แทนการเดาจาก `selectedStudentIds`: `triggerExport/triggerPrintCards/triggerRegisExport/triggerPrintTranscript/triggerPrintGraduationCert('filter'|'selection')`, `triggerBulkPromotion('store'|'selection')`, `triggerMoveClassroom('room'|'selection')`
- [x] `triggerMoveClassroom('room')` (จาก dropdown) เลิก fallback ไปห้องแรกที่มีคนแบบเดาสุ่มเมื่อไม่มี chip ห้องเลือกอยู่ — เมนูถูก disable ไว้แล้วในกรณีนั้น (บังคับติ๊กเฉพาะ "ย้ายห้องเรียน" ตามที่ผู้ใช้ขอ ส่วน "เลื่อนชั้น" ยังยกทั้งโรงเรียนได้เหมือนเดิมเพราะเป็น flow ปกติสิ้นปี)
- [x] แก้ scope ที่ "หลอก" 2 จุด: `triggerPrintCards` ใช้คำอธิบายเดียวกับ `triggerExport` แทนคำว่า "ทั้งหมด" ที่ไม่ตรงกับตัวเลขจริง, `triggerPrintTranscript`/`executePrintTranscript` เลิกทิ้ง `#transcript-target` ไว้เฉยๆ (เลือกตัวเลือกไหนก็ได้ผลเหมือนกันหมด) ให้อ่านค่าจริงและคำนวณรายชื่อตามกลุ่มเป้าหมายที่เลือก (ที่เลือก/ตามตัวกรอง/สำเร็จการศึกษา/กำลังศึกษาทั้งหมด)
- [x] ขยายขอบเขต ปพ.2: เพิ่ม dropdown "กลุ่มเป้าหมาย" (สำเร็จการศึกษาแล้ว / ยังไม่ได้ทำเรื่องจบ (กำลังศึกษา) / ทั้งสองสถานะ — โชว์ทั้ง 2 scope ต่างจากเดิมที่ซ่อนไปเลยตอนติ๊กเลือกไว้) และขยายระดับชั้นจาก ม.3/ม.6 เป็น ม.3–ม.6 ครบ; แก้บั๊ก `getCertTargetsBase()` เดิมที่บังคับ `status==='graduated'` ตายตัวทุกกรณี ทำให้ติ๊กคนที่ยังไม่จบไว้แล้วรายชื่อเหลือ 0 คนเงียบๆ โดยไม่บอกเหตุผล; เพิ่ม badge `⚠ ยังไม่จบ` ต่อท้ายชื่อ + แถบเตือน amber นับจำนวนคนที่ยังไม่จบ + empty state ที่บอกเหตุผลว่ากรองด้วยอะไรจนเหลือ 0
- [x] ล้าง id ผี `#manage-dropdown` (ค้างอยู่ใน `DROPDOWN_IDS`, selector คลิกนอกกรอบ, CSS z-index list, และ `toggleManageDropdown()`) ทั้งที่ไม่มี element นี้ในหน้าเลย — และรวม selector คลิกนอกกรอบให้สร้างจาก `DROPDOWN_IDS` ตัวเดียวกันแทนการฮาร์ดโค้ดซ้ำ 2 ที่

# task-10-2026-09-07
- [x] แก้ default sort ให้ครบ 3 ระดับ: ชั้น → ห้อง → **เลขที่นักเรียน** (เดิม comparator ของ key `classroom` เรียงแค่ชั้น+ห้อง คนในห้องเดียวกันเลยคงลำดับเดิมในอาร์เรย์ ≈ ลำดับที่เพิ่มเข้าระบบ ทำให้ดูเหมือนเรียงตามเวลาที่เพิ่ม) — ยืนยันด้วยสคริปต์รันจริงก่อนแก้ พบ ม.1/1 ขึ้นเลขที่ 11 ก่อน 1,2,3
- [x] แก้ `syncSortForScope()` ไม่ให้สลับไป "เลขที่ในห้อง" อัตโนมัติเมื่อ sort ปัจจุบัน (`classroom` asc) ให้ผลเหมือนกันอยู่แล้วหลังแก้ข้อบน — กันป้าย/badge "อัตโนมัติ" ขึ้นทั้งที่ลำดับแถวไม่ขยับ
- [x] แก้บั๊ก "ย้ายห้องเรียน" จาก dropdown กดแล้วไม่มีอะไรเกิดขึ้น — ต้นเหตุคือ task-09 บังคับต้องเลือก chip ห้องก่อนถึงจะปลดล็อกปุ่ม (ตรวจสอบด้วย jsdom แล้วว่า `<select>` ห้องต้นทางในโมดัลใช้งานได้ปกติ 100% การบล็อกจึงไม่มีประโยชน์ มีแต่ทำให้ดูเหมือนพัง) — เพิ่ม `resolveDefaultMoveRoom()` (chip ที่กรองอยู่ถ้ามี ไม่มีก็ห้องแรกที่มีคน) ให้ทั้งป้ายเมนูและตัว trigger ใช้ค่าเดียวกัน, กฎ "dropdown=ยกทั้งห้อง / bulk bar=เฉพาะที่เลือก" ของ task-09 ยังคงเดิม
- [x] ถอดกลไก `disabled` ออกจากระบบป้ายเมนูทั้งหมด (`getActionScope()`/`refreshActionLabels()`) — แถวที่ "ยังทำไม่ได้" ให้กดได้เสมอแล้วอธิบายด้วย popup ขวางหน้าแทนแถวเทาๆ ที่กดแล้วเงียบ (แก้ `moveClassroom` และ `transferOut` พร้อมกันเพราะเป็นปัญหาเชิงระบบเดียวกัน)
- [x] เพิ่ม `openWarningNotice(title, message)` ห่อ `openNotice()` เดิม (จาก task-08) แล้วแปลงทั้ง **10 จุด** ที่เคยเป็น `showToast(..., 'warning')` (dead-end เช่น "ยังไม่ได้เลือกนักเรียน", "ไม่มีรายชื่อที่จะดำเนินการ") ให้เป็น popup ขวางหน้าที่ต้องกด "ตกลง" เท่านั้น — ซ้อนทับโมดัลที่เปิดค้างได้ (`#modal-notice` z-index สูงกว่า) โดยฟอร์มที่กรอกไว้ไม่หาย, ไม่แตะ `showToast(..., 'success'|'info')` ทั้ง 19 จุดตามที่ผู้ใช้ระบุ
- [x] เพิ่ม guard กัน action รันกับ 0 รายการแบบไม่มีความหมาย (`triggerExport`, `triggerPrintCards`, `triggerRegisExport`, `executePrintTranscript`) — ขึ้น popup เตือนแทน toast "0 รายการ"

# task-11-2026-09-07
- [x] **Feature 2 (ตั้งค่าคอลัมน์ตาราง):** เพิ่ม config คอลัมน์ตารางข้อมูลนักเรียนแบบใหม่ทั้งชุด — `defaultStudentColumns` + `getStudentColumns`/`saveStudentColumns`/`resetStudentColumns` ใน `js/store.js` (แพตเทิร์นเดียวกับ `admitify_exam_rooms`), แล้วทำให้ตารางใน `students.html` เรนเดอร์หัวตาราง (`renderTableHead()`) และแต่ละเซลล์ (`buildColumnCell()` + `getByPath()`) ตาม config จริงแทนการฮาร์ดโค้ด 11 `<th>`/`<td>` คงเดิม 2 ที่ที่ต้องตรงกันเอง — แก้ `colspan` ของ empty state ให้คำนวณจากจำนวนคอลัมน์ที่แสดงจริงด้วย
- [x] สร้างหน้าใหม่ `student_field_settings.html` (โคลนโครงจาก `course_settings.html`) ให้ครู เพิ่ม/แก้ไข/ลบคอลัมน์ได้, ปิด-เปิดการแสดงผล, เลื่อนลำดับซ้าย-ขวา, และ "ตั้งเป็นคอลัมน์หลัก" (มีได้ทีละ 1 คอลัมน์ ซ่อนไม่ได้ เป็นเซลล์ที่คลิกเปิดโปรไฟล์) — คอลัมน์มาตรฐานเดิม (`locked:true`) แก้ได้แค่ชื่อที่แสดง ลบไม่ได้ เพราะ field/การจัดวางไม่มีผลกับ renderer ของคอลัมน์เหล่านั้น (ผูกกับ `column.id` ตรงๆ), ปุ่ม "คืนค่าเริ่มต้น" เรียก `resetStudentColumns()`
- [x] เพิ่มไอคอนเกียร์ต่อท้ายหัวข้อ "ข้อมูลรายละเอียดนักเรียน" ลิงก์ไปหน้าใหม่ + เพิ่ม pill ที่ 4 "ตั้งค่าคอลัมน์นักเรียน" ใน sub-nav ของ `settings.html`/`course_settings.html`/`room_exam_settings.html` (ขยาย wrapper จาก `max-w-md` เป็น `max-w-2xl`)
- [x] **Feature 1 (ส่งออกรายชื่อนักเรียนแยกตามห้อง):** เมนูใหม่ใน dropdown "นำเข้า/ส่งออก" — เลือกประเภทใบรายชื่อ (ทั่วไป/ใบเช็คชื่อ/มีข้อมูลติดต่อ/กำหนดเอง พร้อม preset คอลัมน์), ติ๊กคอลัมน์จาก config เดียวกับ Feature 2, ติ๊กห้องเรียนได้หลายห้อง (ค่าเริ่มต้นตาม chip ชั้น/ห้องที่กรองอยู่) — ห้องเดียวมีปุ่มส่งออกตรง Excel/PDF, ตั้งแต่ 2 ห้องขึ้นไปเปิดโมดัลเลือกรูปแบบไฟล์ร่วม, ไม่เลือกห้องเลยขึ้น popup เตือนแทนปุ่มที่กดแล้วเงียบ
- [x] เพิ่มโมดัลเลือกรูปแบบไฟล์ที่ใช้ร่วมกัน (`openExportFormatModal`) — PDF/Excel × รวมเป็นไฟล์เดียว(แยกหน้า/แยก sheet ตามชนิดไฟล์)/แยกหลายไฟล์ พร้อมพรีวิวชื่อไฟล์จริงที่จะได้ — ทุก export ในฟีเจอร์นี้ยัง mock (toast + activity log) เหมือน export อื่นทั้งหน้า
- [x] **Feature 3 (Export รายงานลาออก/รายงานนักเรียนใหม่):** เพิ่ม 2 เมนูใหม่ใน dropdown "รายงาน/สถิติ" — เลือกปีการศึกษา/ภาคเรียนแล้วกรองนักเรียนจริงจาก store (`academicPeriodRange()`/`inAcademicPeriod()` แปลงปี พ.ศ.+เทอมเป็นช่วงวันที่ตามปฏิทินโรงเรียนไทย: เทอม 1 พ.ค.-ก.ย., เทอม 2 ต.ค.-มี.ค., ฤดูร้อน เม.ย.) แสดงจำนวนจริงแยกตามระดับชั้น, ไม่มีคนตรงเงื่อนไขขึ้น popup เตือนพร้อมบอกเหตุผล, `#dd-reports` ยังไม่มี badge ตามกติกา task-09 เพราะขอบเขตมาจากปี/เทอมในโมดัล ไม่ใช่ selection/ตัวกรอง
- [x] ตรวจสอบด้วย jsdom ครบทุกฟีเจอร์ (216 เช็ครวม 5 ชุดทดสอบ ผ่านหมด ไม่มี regression กับ task-08/09/10)

# task-12-2026-09-08
- [x] รวม "ส่งออกข้อมูล (Excel)", "ส่งออกรายชื่อนักเรียน (แยกตามห้อง)", "Export รายงานลาออก", "Export รายงานนักเรียนใหม่" ที่เคยแยกกันคนละเมนู (และบางอันมีขั้นที่ 2 ซ้อนโมดัล) เป็นโมดัลเดียว อ่านจากบนลงล่าง 4 บล็อก: ①ประเภทข้อมูล ②ขอบเขต (ตามตัวกรอง/ที่เลือก/เลือกห้องเอง หรือปี-เทอมสำหรับรายงาน) ③คอลัมน์ ④รูปแบบไฟล์ — ยุบ `openExportFormatModal`/`renderListExportModal`/`renderReportExportModal` เดิมเข้าเป็น `renderExportModal()`/`confirmExport()` ตัวเดียว ใช้ state กลาง `exportState`
- [x] dropdown "นำเข้า/ส่งออก": เพิ่มเส้นแบ่งระหว่างกลุ่ม "นำเข้า" กับ "ส่งออก & พิมพ์" (พร้อม label กลุ่ม), ลบเมนู "ส่งออกรายชื่อนักเรียน (แยกตามห้อง)" ที่แยกออกไปแล้ว (รวมเข้าโมดัล export ด้านบนแทน), เปลี่ยน label "ส่งออกข้อมูล (Excel)" → "ส่งออกข้อมูล" (เลือก PDF/Excel ในโมดัลแล้ว)
- [x] dropdown "รายงาน/สถิติ" เหลือรายการเดียว "รายงานสถิตินักเรียนรายชั้น" — หุบ "พิมพ์รายงานสรุปสถานะ"/"Export รายงานลาออก"/"Export รายงานนักเรียนใหม่" เข้าไปเป็นทางลัดในโมดัลรายงานสถิติรายชั้นแทน (บล็อก "รายงานอื่น ๆ") เผื่อมี custom report เพิ่มในอนาคต — ปุ่ม Export รายงานลาออก/นักเรียนใหม่ ในโมดัลนี้เรียก `triggerExport('filter', 'reportResigned'|'reportNew')` ไปเปิดโมดัล export รวม (จุดที่สองตามที่ผู้ใช้ระบุ)
- [x] ปุ่ม "ย้ายออก / ลาออก / เปลี่ยนสถานะ" (`transferOut`) กลับไป `disabled` จริงเมื่อยังไม่ได้ติ๊กเลือกนักเรียน (ย้อนกลับ task-10 ข้อ 4 เฉพาะปุ่มนี้ ตามที่ผู้ใช้ยืนยัน) — ยังคงมีป้าย `.dd-action-scope` (`— ต้องติ๊กเลือกก่อน`) + `title` tooltip อธิบายเหตุผลเสมอ ไม่ใช่ disable เงียบๆ, ปุ่มอื่นทั้งหมด (ย้ายห้องเรียน/เลื่อนชั้น/ปพ.1/ปพ.2) ยังกดได้เสมอตามกฎเดิม — อัปเดต `AGENTS.md` บรรทัดกฎ no-disable ให้บันทึกข้อยกเว้นนี้ด้วย
- [x] Comment โค้ดสร้างไฟล์จริงจุดเดียวในหน้า (`downloadImportTemplate()` — Blob CSV) ออกชั่วคราว เปลี่ยนเป็น toast แจ้งดาวน์โหลดเหมือน export อื่นทุกจุด (คง `IMPORT_TEMPLATE_COLUMNS`/sample rows ไว้ใช้ตอนเชื่อม backend จริง)
- [x] ตรวจสอบด้วย jsdom 29 (ติดตั้งชั่วคราวใน scratchpad) — 25 เช็คผ่านหมด: ลำดับ/เส้นแบ่ง dropdown, `ACTION_SCOPES` ไม่มี entry ตาย, โมดัล export รวมเปิดครบ 4 บล็อกในโมดัลเดียว, สลับ type เป็นรายงานแล้วซ่อนบล็อกคอลัมน์/โชว์ปี-เทอมถูกต้อง, guard ห้องว่าง/คอลัมน์ว่างขึ้น popup โดยฟอร์มไม่หาย, `transferOut` disable/enable ตามการติ๊กเลือกถูกต้อง, ไม่มี `new Blob(` ที่ยัง active เหลือในไฟล์, export สำเร็จแล้วปิดโมดัล+บันทึก activity log

# task-13-2026-09-09
- [x] แก้อาการ popup export/print/save "ยืดๆ หดๆ" — ต้นเหตุคือ `#mock-modal-card` (shell กลางที่ทุกโมดัลกลุ่มนี้ใช้ร่วมกัน) เป็นการ์ดที่โตตามเนื้อหาแบบไม่มี transition ทุกครั้งที่สลับตัวเลือกแล้ว `render*Modal()` เขียน `innerHTML` ใหม่ การ์ดจะ snap ขนาดทันที — เปลี่ยนเป็น flex column คงที่ (header/body/footer แยกกัน, มีแค่ body เดียวที่ scroll) แบ่งโมดัลเป็น 2 กลุ่ม: **Dynamic** (re-render ตอนกดตัวเลือก — Export/ปพ.2/ย้ายห้องเรียน/ย้ายออก/เลื่อนชั้น/นำเข้า) ได้ความสูงคงที่ `h-[85vh] max-h-[46rem]` (`MODAL_HEIGHT_DYNAMIC`) ไม่ขยับเลยไม่ว่ากดอะไร, **Static** (เนื้อหานิ่งหลังเปิด — ปพ.1/REGIS/รายงานสถิติ) คงพฤติกรรมโตตามเนื้อหาเดิม (`max-h-[85vh]`) — `setMockModalSize()` เพิ่ม parameter ที่ 2 (heightClass) แบบมี default ทำให้ call site เดิมยังใช้ได้หมด
- [x] แก้ `toggleTransferDestField()` ที่ใช้ `style.display = 'block'/'none'` ตรงๆ (กระตุกหนักสุด) — เปลี่ยนเป็น class `.dd-collapse`/`.dd-collapse-open` ใช้เทคนิค `grid-template-rows: 0fr → 1fr` + `transition` แทน ไม่ผ่าน re-render (กัน cursor เด้งตอนพิมพ์ช่องเหตุผลอยู่)
- [x] ปรับให้กล่องรายชื่อ (ย้ายห้องเรียน/ย้ายออก/ปพ.2) เป็น `flex-1 min-h-0` กินพื้นที่ที่เหลือของโมดัลแทน `max-h-56` ตายตัว — เหลือ scrollbar เดียวจริงๆ (ตัวควบคุมด้านบน+ปุ่มด้านล่างปักอยู่กับที่เสมอ ไม่ต้องมีการ์ดนอกสกอลล์ซ้อนกับลิสต์ในอีกชั้น)
- [x] จัดวางแนวนอนลดความสูงเนื้อหาต่อโมดัล ตามที่ผู้ใช้ขอ: Export (checklist ห้อง+คอลัมน์ วางคู่กัน, radio รวม/แยกไฟล์+สรุปไฟล์ วางคู่กัน), REGIS (ขยาย `max-w-2xl` การ์ด Login/Excel วางคู่กัน), ปพ.2 (ขยาย `max-w-2xl`), ปพ.1 (select+radio วางคู่กัน), ย้ายห้องเรียน (mode toggle + สรุปการย้าย วางคู่กัน), ย้ายออก (select ประเภท + ช่องโรงเรียนปลายทาง วางคู่กัน)
- [x] เพิ่มหน้า **preview ก่อนยืนยันจริง** สำหรับทุก action ที่ export/print/save (ตามที่ผู้ใช้ขอ อ้างอิงฟีเจอร์พิมพ์ใบสมัครของ `index.html`) — โมดัลใหม่ `#modal-action-preview` + ฟังก์ชันกลาง `openActionPreview()`/`buildDocSheet()`/`buildTablePreview()`/`buildIdCardGrid()` แทรกอยู่ระหว่างโมดัลตัวเลือกเดิมกับ mock action เดิม (ไม่แก้ guard/validation หรือข้อความ toast/log เดิม) ครอบคลุม 8 จุด: ส่งออกข้อมูล (`confirmExport`, ตารางเลียนแบบไฟล์จริงจาก `buildColumnCell()`/`buildStudentRowCtx()` ตัวเดียวกับตารางในหน้า), REGIS Excel (`regisExportExcel`), ปพ.2/ปพ.1 (`executePrintCert`/`executePrintTranscript`, การ์ด A4 จำลอง), พิมพ์บัตรนักเรียน (`triggerPrintCards`, เดิมไม่มีโมดัลเลยตอนนี้มี preview เป็นขั้นเดียว), พิมพ์รายงานสรุปสถานะ (`triggerPrintReport`) — และตามที่ผู้ใช้ยืนยันเพิ่ม รวม **ย้ายห้องเรียน/ย้ายออก** ด้วย (แสดงตาราง "ห้องเดิม→ห้องใหม่"/"สถานะใหม่+เหตุผล" ก่อนบันทึกจริง — ยืนยันด้วย jsdom แล้วว่า `window.AdmitifyStore` **ไม่ถูกแก้ไข** จนกว่าจะกดยืนยันในหน้า preview)
- [x] แก้บั๊ก `getExportTargetCount()` scope `rooms` ที่นับจากผลรวม `classroomOptions()` แต่ไม่มีฟังก์ชันไหนคืนรายชื่อนักเรียนจริงเลย (ทำให้ preview เป็นไปไม่ได้) — เพิ่ม `getExportTargets()` คืน student object จริงครบ 3 scope แล้ว refactor `getExportTargetCount()` ให้เรียกจากตัวนี้แทน กันตัวเลขบนปุ่มกับจำนวนแถวใน preview ไม่ตรงกัน
- [x] Refactor ส่วนคำนวณ ctx ต่อแถว (คำนำหน้า/ป้ายสถานะ/วันที่/ห้องเรียน) ที่เคยฝังอยู่ใน `renderTable()` แยกออกเป็น `buildStudentRowCtx()` ใช้ร่วมกับตาราง preview เพื่อให้หน้าตาตรงกับตารางจริงเป๊ะ
- [x] ตรวจสอบด้วย jsdom (ติดตั้งชั่วคราวใน scratchpad, inline `js/store.js`/stub `tailwind`+`lucide` แทนโหลดจริงเพราะไม่มีเน็ตในแซนด์บ็อกซ์) — 15 เช็คผ่านหมด: โครง shell ใหม่ถูกต้อง, โมดัล Dynamic ได้ความสูงคงที่/โมดัล Static ไม่ถูกบังคับความสูง, `toggleTransferDestField` ใช้ class แทน inline style แล้วสลับไปมาได้ถูกต้อง, `getExportTargets`/`getExportTargetCount` ตรงกัน, ทั้ง 8 จุด preview เปิดถูกเนื้อหา (ตาราง/การ์ด A4/กริดบัตร) และปิดกลับไป mock action ถูกต้องหลังยืนยัน, ย้ายห้องเรียน+ย้ายออกยืนยันแล้วว่า store ไม่ถูกแก้ก่อนกด "ยืนยัน" ใน preview และถูกแก้ถูกต้องหลังกด — ยังไม่ได้ทดสอบ layout แนวนอน/แนวตั้งจริงในเบราว์เซอร์ (jsdom ไม่มี CSS layout engine) รอผู้ใช้เปิดไฟล์ตรงๆ คลิกดูจริงอีกที

# task-14-2026-09-21
- [x] แถวห้องเรียนไม่ให้ยิง chip ทั้งโรงเรียน (30+ ห้อง) ทับหน้าตอนเลือกระดับชั้น "ทั้งหมด" อีกต่อไป — `renderClassroomChips()` เช็คก่อนว่ามีชั้นที่เลือกอยู่ไหม ถ้าไม่มี (`activeGrades.size === 0`) เหลือแถว "ห้องเรียน:" ไว้เฉยๆ แต่โชว์ข้อความจางๆ "เลือกระดับชั้น (ม.1 – ม.6) ด้านบนก่อน" แทน ไม่ล้นแถวเหมือนเดิม
- [x] เพิ่มสวิตช์ "เลือกหลายอัน" ข้าง dropdown "เรียงตาม" (แถว 1 ของแถบตัวกรอง) — เปิดแล้ว chip **ทุกแถว** (ระดับชั้น/ห้องเรียน/แผนการเรียน/สถานะ) เลือกได้มากกว่า 1 ค่าพร้อมกัน โดยโหมดปิด (ค่าเริ่มต้น) ยังทำงานเหมือนเดิมเป๊ะ (เลือกได้ทีละค่า)
- [x] เปลี่ยน state ตัวกรอง 4 ตัว (`activeGrade/activeClassroom/activePlan/activeStatus`) จาก scalar เป็น `Set` (`activeGrades/activeRooms/activePlans/activeStatuses`, เซ็ตว่าง = "ทั้งหมด") — เพิ่ม helper กลาง `applyChipSelection()` (toggle เข้า/ออกตอนเปิดโหมด multi, แทนที่ค่าเดิมตอนปิดโหมด) และ `syncChipActiveStates()` แทนการ toggle active class ซ้ำ 4 ที่, เพิ่ม `soleGrade()`/`soleRoom()` ให้จุดที่ยังต้องการ "ค่าเดียว" (โมดัลย้ายห้องเรียน/ค่าเริ่มต้น export) เรียกใช้แทนอ่าน Set ตรงๆ
- [x] ตามแก้จุดที่อ่าน `activeGrade`/`activeClassroom` แบบ scalar เดิมให้ครบ: `getFilteredList()` + สำเนา predicate ใน `updateStatusCounts()`, `syncSortForScope()`, `resolveDefaultMoveRoom()`, `setMoveClassroomMode()`, ค่าเริ่มต้นห้องของโมดัล export — ทุกจุดอ่านจาก Set/helper ใหม่แทน
- [x] จำสถานะสวิตช์ไว้ใน `admitify_students_prefs` (คีย์ `multi`) เหมือน pageSize/sort เดิม — ตัวกรอง chip เองยังไม่จำ (รีโหลดหน้ากลับไปเป็นค่าเริ่มต้นเหมือนเดิม)
- [x] ตรวจสอบด้วย jsdom (ติดตั้งชั่วคราวใน scratchpad, inline `js/store.js`/stub `tailwind`+`lucide`) — 42 เช็คผ่านหมด: โหมดปิดยังเลือกได้ทีละค่าเหมือนเดิมทุกแถว, แถวห้องเรียนว่าง+มีข้อความจางตอนไม่เลือกชั้น, เปิดสวิตช์แล้วเลือกได้หลายชั้น/หลายห้องข้ามชั้น/หลายสถานะพร้อมกัน, ปิดสวิตช์ทั้งที่เลือกหลายค่ายุบเหลือค่าแรกถูกต้อง, `clearAllFilters()` รีเซ็ตเซ็ตทั้งหมดได้ทั้งสองโหมดโดยไม่แตะสวิตช์, `syncSortForScope`/`resolveDefaultMoveRoom`/`soleRoom`/export modal ทำงานถูกต้องกับ Set ใหม่, ตัวเลข `search-count-badge` ตรงกับ `currentFilteredList.length` จริง

# task-15-2026-09-21
- [x] ยกเครื่องโมดัล "ส่งออกข้อมูล" (`renderExportModal()`) จากการ์ดแคบ `max-w-2xl` ที่มี scrollbar ซ้อนกัน 3 ชั้น (ห้องเรียน 30 ห้อง + คอลัมน์ 14 ตัว ต่างยัดใน `max-h-52 overflow-y-auto`, แล้วทั้งโมดัลยัง scroll ซ้อนอีกชั้น) เป็นเลย์เอาต์ 3 คอลัมน์ตาม `materials/mockup/image.png` — ขยายเป็น `max-w-6xl` + เพิ่มค่าคงที่ความสูงใหม่ `MODAL_HEIGHT_TALL` (`h-[88vh] max-h-[50rem]`, คงที่ตามกฎเดิมไม่ยืดหด) ให้เนื้อหาพอดีโดยไม่ต้อง scroll: **ซ้าย** = ①ประเภทข้อมูล/②ขอบเขต/③รูปแบบไฟล์ เป็น radio สั้นๆ คุมได้ในตัวเอง, **กลาง** = ตารางห้องเรียน + ตารางคอลัมน์ (ของที่กินพื้นที่ โชว์ค้างตลอด), **ขวา** = live preview ย่อ + สรุปไฟล์ที่จะได้ (อัปเดตสดทุกครั้งที่กดตัวเลือก, ใช้ `buildStudentRowCtx()`/`buildColumnCell()` ตัวเดียวกับตารางจริง)
- [x] ตารางห้องเรียนแบบใหม่ — เลิกลิสต์ checkbox แนวตั้ง 30 บรรทัด เปลี่ยนเป็น grid ตามโครงสร้างจริง 6 แถว (ม.1–ม.6) × 5 ช่อง (ใช้ `.filter-chip` เดิม) พร้อมปุ่ม "ทั้งชั้น" ต่อแถว (`toggleExportRoomsByGrade()` ใหม่) — ยังคง "เลือกทั้งหมด"(sky-600)/"ล้าง"(rose-600) ตามคอนเวนชันเดิม, ห้องนอกมาตรฐานต่อท้ายเป็นแถว "อื่นๆ"
- [x] **Smart default scope:** เดิม `triggerExport()` default เป็น "ตามตัวกรองปัจจุบัน" เสมอไม่ว่าจะกรองอะไรอยู่หรือไม่ (การ pre-seed ห้องตามตัวกรองมีอยู่แล้วแต่ทำงานเฉพาะ `type==='roster'`) — เพิ่ม `resolveDefaultExportScope()` ใช้ร่วมกันทั้ง `type='students'` และ `'roster'`: ค้นหา/กรองแผนการเรียน/กรองสถานะที่ไม่ใช่ "กำลังศึกษา" → ไปที่ "ตามตัวกรอง" (ครูตั้งใจเจาะจงอยู่แล้ว), นอกนั้น (ไม่กรองเลย หรือกรองแค่ชั้น/ห้อง) → ไปที่ "เลือกห้องเรียนเอง" พร้อมติ๊กห้องที่กรองอยู่ให้ (ไม่กรองเลย = ไม่ติ๊กห้องไหน กันส่งออกยกโรงเรียนโดยไม่ตั้งใจ), `requestedScope==='selection'` (จาก bulk-action-bar) ยังเคารพตรงๆ เสมอ — ประเภท "รายงาน" ข้าม logic นี้เพราะไม่มีขอบเขตแบบห้อง/ตัวกรอง, `setExportType()` ไม่ re-run smart default ระหว่างเปิดโมดัลอยู่ (คงพฤติกรรมเดิม)
- [x] จัดคอลัมน์ที่จะแสดง (14 ตัว) เป็นกริด 2 คอลัมน์แทนลิสต์แนวตั้งเดิม ให้พอดีในพื้นที่โดยไม่ scroll — รวมแถวหัวกล่อง (จำนวนคอลัมน์ + preset ใบรายชื่อ) เป็นแถวเดียวประหยัดพื้นที่แนวตั้ง
- [x] ไม่แตะ `confirmExport()`/`openActionPreview()`/guard 4 ตัว/`ACTION_SCOPES`/`exportState` schema เดิม — งานนี้เป็นการจัดวางใหม่ ไม่เปลี่ยน data model, ขั้น preview เต็มจอก่อนยืนยันจริงยังคงอยู่ตามกฎเดิม (ดู AGENTS.md), เคส "รายงาน" (reportResigned/reportNew) มี degenerate layout แยก (กลาง = ปี/เทอม + สรุปตามระดับชั้น, ไม่มี ② ในรางซ้าย)
- [x] ตรวจสอบด้วย jsdom (ติดตั้งชั่วคราวใน scratchpad) — 34 เช็คผ่านหมด: `resolveDefaultExportScope()` ครบ 6 เคส (ไม่กรอง/ค้นหา/กรองแผน/กรองสถานะแปลก/กรองชั้น/กรองห้อง), `triggerExport()` ผูก smart default เข้าทั้ง students/roster และข้าม report ถูกต้อง, `toggleExportRoomsByGrade()` ติ๊ก/ถอนทั้งชั้นถูกต้อง, การ์ดใช้ `max-w-6xl`/`MODAL_HEIGHT_TALL` จริง, ตารางห้องเรียนโชว์เฉพาะเลขห้อง (tooltip มีชื่อเต็ม), `confirmExport()` guard เดิมยังทำงาน, `getExportTargets`/`buildExportFileNames`/`buildExportMiniPreview` ถูกต้องทั้งเคสว่างและมีข้อมูล — ยังไม่ได้ทดสอบว่า "ไม่มี scroll จริง" ในเบราว์เซอร์ (jsdom ไม่มี CSS layout engine) รอผู้ใช้เปิดไฟล์ตรงๆ ดูจริง

## task-15b (ต่อจาก task-15 วันเดียวกัน — ผู้ใช้ลองของจริงแล้วขอแก้ 2 จุด)
- [x] **บีบคอลัมน์กลาง คืนพื้นที่ให้ preview** — เดิมกลางเป็น `minmax(0,1fr)` (ยืดกินพื้นที่ที่เหลือ) ขวาถูกล็อก `16rem` (256px) ทำให้ preview แคบจนอ่านไม่ออก — สลับสัดส่วน: กลางล็อกเป็น **`21rem` คงที่** (พอสำหรับตารางห้อง+คอลัมน์), ขวาเปลี่ยนเป็น **`minmax(0,1fr)`** (ยืดกินพื้นที่ที่เหลือแทน ~624px จาก 256px) พร้อมขยายโมดัลจาก `max-w-6xl` → **`max-w-7xl`** ให้มีพื้นที่รวมพอ — เพิ่ม `min-h-0` ให้ทั้ง 3 รางด้วย (CSS grid item ค่าเริ่มต้น `min-height:auto` ทำให้ `overflow-y-auto` ไม่ทำงานจริงถ้าไม่ใส่ ก่อนหน้านี้ไม่เห็นปัญหาเพราะเนื้อหาสั้น)
- [x] **preview เห็นครบทุกหน้า ไม่ตัดทิ้ง** — เดิม `buildExportMiniPreview()` ตัดทิ้ง 2 แกน (6 แถวแรก × 4 คอลัมน์แรก + ข้อความ "…และอีก N รายการ/คอลัมน์") — เขียนใหม่เป็น `buildExportPagedPreview()`: จำลอง "กองหน้ากระดาษ" แบบ print preview ของ `index.html` (พื้นโต๊ะเทา `bg-slate-100` + กระดาษขาว `shadow-lg`) แบ่งหน้าจริงด้วยค่าคงที่ใหม่ `EXPORT_PREVIEW_ROWS_PER_PAGE = 20` แถว/หน้า พร้อมเลขหน้า "หน้า N/M" ต่อกระดาษ (ของใหม่ — ทั้งโปรเจกต์ไม่เคยมีเลขหน้าในกระดาษจำลองมาก่อน) — ใช้กระดาษสัดส่วน A4 responsive (`aspect-[210/297]` กว้างเต็มราง) แทน `w-[210mm]` ตายตัวแบบ `buildDocSheet()` เดิม เพราะตรวจแล้วทั้งโปรเจกต์ไม่มีกลไก scale-to-fit ให้ใช้ร่วม (ไม่มี `zoom:`/`transform: scale` ที่ไหนย่อกระดาษเลย)
- [x] **โหมด "แยกเป็นหลายไฟล์" แยกหัวข้อตามไฟล์ใน preview** — เพิ่ม `getExportSplitGroups()` (คืนกลุ่ม "ถ้าแยกไฟล์จริง" ไม่สนโหมดปัจจุบัน — เหมือนพฤติกรรมเดิมของ `getExportFileCount()` ที่ไม่เคยดู `exportState.mode`) และ `getExportFileGroups()` (กลุ่ม "จริง" ตามโหมดปัจจุบัน — merge=กลุ่มเดียวรวมทุกคน, split=แยกตาม `getExportSplitGroups()`) — refactor `getExportFileCount()` ให้เรียกจาก `getExportSplitGroups().length` แทนคำนวณเอง (ตัวเลขเดิมเป๊ะ ไม่เปลี่ยนพฤติกรรม) กันจำนวนไฟล์/หัวข้อ preview/`buildExportFileNames()` ไม่ตรงกัน — scope `rooms` แยกทีละห้อง (เรียงด้วย `classroomSortParts` ให้ตรงกับเลข `_1.._N`), รายงานแยกทีละระดับชั้นที่พบ, ขอบเขตอื่นแยกไม่ได้เป็นกลุ่มเดียวเสมอ — เลข "ลำดับ" นับใหม่ทุกไฟล์ (ตรงกับพฤติกรรมไฟล์จริง)
- [x] ไม่แตะ `confirmExport()`/`openActionPreview()`/guard 4 ตัว ตามที่ผู้ใช้ยืนยัน — โมดัล preview เต็มจอตอนกด "ส่งออก" ยังทำงานเหมือนเดิมทุกประการ (ยังใช้ `buildTablePreview()` สุ่ม 15 แถวเหมือนเดิม ไม่ได้เปลี่ยนตาม preview ใหม่ในรางขวา)
- [x] ตรวจสอบด้วย jsdom (`test-export-modal.js` เดิมในสแครชแพด, อัปเดตเพิ่ม 13 เช็คใหม่) — **47/47 ผ่าน**: การ์ดใช้ `max-w-7xl` จริง, grid template ใหม่ + ทั้ง 3 รางมี `min-h-0`, `buildExportPagedPreview()` ไม่มีคำว่า "และอีก" หลงเหลือเลย, จำนวนหน้า = `ceil(targets/20)` เป๊ะ, จำนวน `<table>` ที่เรนเดอร์ = จำนวนหน้าเป๊ะ (ไม่มีหน้าไหนถูกข้าม/ซ้ำ), `getExportFileCount()`/`getExportSplitGroups()`/`getExportFileGroups()` ตรงกันทั้ง merge/split/รายงาน, ผลรวมแถวของทุกกลุ่มเท่ากับ targets ทั้งหมด (ไม่มีใครหาย/ซ้ำ), โหมดแยกไฟล์มีหัวข้อไฟล์สเปรดชีตครบทุกไฟล์ — รัน `test-multifilter.js` ซ้ำด้วย ไม่มีอะไรถดถอย (42/42) — ยังไม่ได้ดูจริงในเบราว์เซอร์ว่ากระดาษสัดส่วน A4 responsive หน้าตาเป็นยังไงจริง (jsdom ไม่มี CSS layout engine) รอผู้ใช้เปิดไฟล์ตรงๆ ดู

## task-15c (ต่อจาก task-15b วันเดียวกัน — ผู้ใช้แจ้งกฎโดเมนที่ขาดไป: ใบรายชื่อนักเรียน = 1 ห้อง 1 แผ่นเสมอโดยธรรมชาติ ยกเว้นงานเฉพาะทาง เช่น ใบเช็คชื่อกีฬาสี ที่รวมหลายห้องไว้แผ่นเดียว)
- [x] **ยุบ `exportState.mode` (`'merge'`/`'split'`) เป็น `exportState.layout` เดียว 3 ค่า** — `'sheet-per-room'` (ค่าเริ่มต้นใหม่ — ไฟล์เดียว ขึ้นแผ่นใหม่ทุกห้อง/ระดับชั้น ตรงกับป้ายเดิม "แยก sheet"/"แยกหน้า" ที่สัญญาไว้แต่โค้ดไม่เคยทำจริง), `'one-sheet'` (ของใหม่ — ไฟล์เดียว รวมทุกห้องต่อกันในแผ่นเดียว สำหรับงานอย่างเช็คชื่อกีฬาสี), `'file-per-room'` (เดิม `'split'` — แยกไฟล์ตามห้อง/ชั้น) — `setExportMode()` → `setExportLayout()`, เลือก `'one-sheet'` แล้วติ๊กคอลัมน์ "ชั้นเรียน" (`classroom`) ให้อัตโนมัติ (ไม่งั้นแผ่นรวมไม่รู้ว่าใครห้องไหน — รายงานมีคอลัมน์นี้อยู่แล้วใน `DEFAULT_REPORT_EXPORT_COLUMNS` จึงข้าม)
- [x] **`getExportSplitGroups()` เดิม (task-15b) → `getExportGroups()`** — เปลี่ยนจากแบ่งกลุ่มได้เฉพาะตอน `scope==='rooms'` เป็น **แบ่งตามห้องจริงของนักเรียนแต่ละคนเสมอ ไม่ว่าจะเลือกขอบเขตแบบไหน** (ตามที่ผู้ใช้ยืนยัน — `scope='filter'`/`'selection'` ก็ยังต้องแบ่งแผ่นตามห้องจริงของแต่ละคน) ประเภทรายงานยังแบ่งตามระดับชั้นเหมือนเดิม (ไม่มีห้องให้แบ่ง)
- [x] เพิ่ม `getExportStructure()` — โครงผลลัพธ์ 3 ชั้น `[{ fileName, sheets: [{ label, rows }] }]` ตาม layout ปัจจุบัน ให้ `getExportFileCount()`/`buildExportFileNames()`/`buildExportPagedPreview()` derive จากตัวเดียวนี้ทั้งหมด (แพตเทิร์นเดียวกับ `getExportTargets()` ใน task-13) ยุบ `getExportFileGroups()` เดิมทิ้ง — **จุดที่ต้องระวังซ้ำ**: ป้ายตัวเลือก "แยกไฟล์ตามห้อง (N ไฟล์)" และเงื่อนไขซ่อนบล็อกตัวเลือกทั้งก้อน ต้องใช้ `getExportGroups().length` (จำนวนกลุ่มสูงสุดที่เป็นไปได้เสมอ) ไม่ใช่ `getExportFileCount()` (จำนวนไฟล์ตาม layout ปัจจุบัน) — ไม่งั้นเลือกโหมดไฟล์เดียวอยู่แล้วบล็อกตัวเลือกทั้งก้อนจะหายไปเอง (กับดักเดียวกับที่เจอใน task-15b)
- [x] `buildExportPagedPreview()` เปลี่ยนจากวนกลุ่มแบนราบเป็นวน `getExportStructure()` 3 ชั้น (ไฟล์→แผ่น→หน้า) — เลข "ลำดับ" เปลี่ยนจากนับใหม่ทุก**ไฟล์** (task-15b) เป็นนับใหม่ทุก**แผ่น** (ตรงกับใบรายชื่อจริงที่แต่ละห้องเริ่มนับ 1 เอง แม้อยู่ไฟล์เดียวกันกับห้องอื่นก็ตาม) — บล็อก ③ รูปแบบไฟล์ในรางซ้ายเปลี่ยนจาก radio 2 ตัว (รวม/แยก) เป็น radio 3 ตัว พร้อมป้ายปรับตามชนิดข้อมูล (ห้อง/ระดับชั้น) และชนิดไฟล์ (sheet/หน้า)
- [x] เพิ่ม `describeExportLayout()` ให้ activity log/toast ใช้คำอธิบาย layout ที่ถูกต้องทั้ง 3 แบบ (เดิมมีแค่ 2 ทาง merge/split) — ไม่แตะ `confirmExport()`/`openActionPreview()`/guard 4 ตัว/`ACTION_SCOPES` นอกเหนือจากบรรทัด log นี้
- [x] ตรวจสอบด้วย jsdom (`test-export-modal.js` เดิม, อัปเดตเพิ่ม 16 เช็คใหม่ครอบคลุม layout ทั้ง 3 แบบ) — **63/63 ผ่าน**: ค่าเริ่มต้น `sheet-per-room` ให้ pageCount ตรงกับผลรวม `ceil(ห้องละ/20)` จริง (มากกว่าแบบรวมทุกห้อง พิสูจน์ว่าไม่ได้ไล่ต่อเนื่อง), `one-sheet` รวมเป็นแผ่นเดียวจริง (pageCount น้อยกว่า) + ติ๊กคอลัมน์ `classroom` ให้อัตโนมัติจริง (ทดสอบจาก preset "ใบเช็คชื่อ" ที่ไม่มีคอลัมน์นี้มาก่อน), `file-per-room` แยกไฟล์ถูกต้อง (ผลรวมแถวเท่ากับ targets ทั้งหมด ไม่มีใครหาย/ซ้ำ), `scope='filter'` ก็ยังแบ่งกลุ่มตามห้องจริง (ไม่ใช่กลุ่มเดียวแบนราบแบบ task-15b), รายงานแบ่ง/รวมตามระดับชั้นถูกต้องทั้ง 2 layout — รัน `test-multifilter.js` ซ้ำด้วย ไม่มีอะไรถดถอย (42/42) — ยังไม่ได้ดูจริงในเบราว์เซอร์ (jsdom ไม่มี CSS layout engine) รอผู้ใช้เปิดไฟล์ตรงๆ ดู

# task-16-2026-09-22
- [x] **เพิ่มโมดัลส่งออก "แบบ B" (2 คอลัมน์สไตล์ Notion/Airtable)** ตาม spec ที่ผู้ใช้ให้มา ควบคู่กับแบบเดิม ("แบบ A" 3 คอลัมน์ของ task-15/15b/15c) **โดยไม่ลบแบบเดิม** — เพิ่มปุ่มสลับ A↔B ในสล็อตใหม่ `#mock-modal-header-slot` (ข้างปุ่ม X ปิดโมดัล ตามที่ผู้ใช้ระบุ) ให้ทดสอบเปรียบเทียบกันได้จริงก่อนตัดสินใจว่าจะเก็บแบบไหน — จำค่าที่เลือกไว้ใน `admitify_students_prefs` (คีย์ใหม่ `exportVariant`)
- [x] `renderExportModal()` เดิมทั้งก้อน (task-15/15b/15c) เปลี่ยนชื่อเป็น **`renderExportModalClassic()`** ("แบบ A") แล้วเพิ่ม `renderExportModal()` ใหม่เป็นตัวกระจายงานตาม `exportVariant` — call site เดิมทั้ง 14 จุด (`setExport*`/`toggleExport*`) ไม่ต้องแก้เลยเพราะยังเรียกชื่อเดิม
- [x] **`renderExportModalTwoCol()`** ("แบบ B") — `max-w-5xl` + ค่าคงที่ความสูงใหม่ `MODAL_HEIGHT_MEDIUM` (`h-[700px] max-h-[90vh]`) เลย์เอาต์ซ้าย ~55% (scroll)/ขวา ~45% (นิ่ง) ตาม spec: ①ชนิดเอกสาร (การ์ด radio ใหญ่กว่าแบบ A ให้ต่างสไตล์ตั้งใจ) ②ปรับแต่งคอลัมน์ (มีปุ่ม "เลือกทั้งหมด/ล้างค่า" ตาม spec — `setExportColumnsAll()` ใหม่) ③รูปแบบไฟล์ ④การจัดการไฟล์ **⑤ขอบเขต (เพิ่มเองนอก spec — spec มีแค่ 4 section ไม่มีวิธีเลือกห้องเลย เทียบกับแบบ A ไม่ได้ถ้าไม่มี)** — ขวา: `buildExportSingleSheetPreview()` การ์ด A4 ตัวแทน **แค่แผ่นแรก** + กล่องสรุปตัวเลข (ต่างจากแบบ A ที่โชว์ครบทุกหน้า — นี่คือส่วนต่างหลักที่ตั้งใจให้ต่างกันเพื่อเทียบ)
- [x] **Refactor ให้ A/B ใช้โค้ดร่วมกัน ไม่ก๊อปซ้ำ** — ดึงออกมาเป็นฟังก์ชันกลาง: `buildExportRoomGridHtml()` (ตารางติ๊กห้อง 6×5 เดิมของแบบ A), `buildExportFileTypeButtonsHtml()`/`buildExportLayoutOptionsHtml()` (ปุ่มชนิดไฟล์ + 3 ตัวเลือก layout เดิมของแบบ A), `buildExportColumnCheckboxHtml(col, compact)` (checkbox คอลัมน์ 1 แถว, `compact` ควบคุมความหนาแน่นให้ A/B ต่างสไตล์ได้), `renderExportVariantToggleHtml()` (ปุ่มสลับ A/B เอง)
- [x] **คอลัมน์เสมือน "ช่องสำหรับเซ็นชื่อ"/"หมายเหตุ"** (`EXPORT_VIRTUAL_COLUMNS`) — ไม่มีข้อมูลจริงรองรับในระบบ (ตรวจแล้ว: applicant ไม่มีฟิลด์หมายเหตุ และข้อความเดิม `ไฟล์ใบเช็คชื่อจะมีคอลัมน์ "ลายเซ็น" เพิ่มให้อัตโนมัติ` เป็นแค่คำโฆษณา ไม่เคยมีโค้ดทำจริง — ลบข้อความนี้ทิ้งเพราะตอนนี้เป็นคอลัมน์เลือกได้จริงแล้ว) — เพิ่ม escape hatch `column.virtual` บรรทัดแรกของ `buildColumnCell()` ให้เรนเดอร์กล่องว่าง/ช่องว่างแทนที่จะตกไป `default:` แล้วได้ `—` ผิดความหมาย
- [x] **คอลัมน์ฐานล็อก** (`EXPORT_BASE_COLUMN_IDS = ['seq','firstName','lastName']`) ตาม spec — `toggleExportColumn()` ปฏิเสธการติ๊กออก, checkbox เรนเดอร์ `disabled checked` ทั้ง A/B, เพิ่ม `ensureExportBaseColumns()` เรียกทุกจุดที่ reassign `exportState.columnIds` (กันคอลัมน์ฐานหลุดถ้า preset/ค่าเริ่มต้นในอนาคตลืมใส่)
- [x] **เพิ่ม CSV** เข้า `exportState.fileType` (เดิมมีแค่ pdf/excel) — `EXPORT_FILE_EXT` map เป็นจุดเดียวที่แปลง fileType→นามสกุลไฟล์ (จุดเดียวในโค้ดทั้งหมด) — edge case: เลือก CSV แล้ว (1) ถอดคอลัมน์ "ช่องเซ็นชื่อ" ออกอัตโนมัติ + ซ่อน checkbox นั้นไปเลย (ไฟล์ข้อความล้วนไม่มีที่ให้เซ็น) (2) ตัวเลือก `sheet-per-room` ถูก disable พร้อม tooltip อธิบาย และถ้าเลือกค้างอยู่ตอนสลับมา CSV จะสลับไป `file-per-room` ให้อัตโนมัติ (3) ป้าย "sheet/หน้า" เพิ่มคำว่า "ตาราง" สำหรับ CSV
- [x] เพิ่ม `getExportSelectableColumns()`/`getExportColumns()` เป็นแหล่งเดียวที่รวมคอลัมน์จริง+เสมือน แล้วให้ `buildExportPagedPreview()`, `buildExportSingleSheetPreview()`, และ `confirmExport()` (จุดที่เคย `studentColumns.filter(...)` ตรงๆ) เรียกจากตัวนี้ทั้งหมด กันคอลัมน์เสมือนหลุดไปจากบางจุด
- [x] ไม่แตะ `confirmExport()` flow/guard 4 ตัว/`openActionPreview()`/`getExportGroups()`/`getExportStructure()`/`ACTION_SCOPES`/`js/store.js` — งานนี้เป็นการเพิ่มเลย์เอาต์ทางเลือกคู่ขนาน ไม่เปลี่ยน data model หรือ flow ยืนยันเดิม
- [x] ตรวจสอบด้วย jsdom (`test-export-modal.js` เดิม, อัปเดตเพิ่ม 29 เช็คใหม่) — **92/92 ผ่าน**: สลับ A↔B ได้จริงไม่หายค่า (`exportState` ก้อนเดียวกัน), จำค่าไว้ใน prefs ถูกต้อง, สล็อตปุ่มหัวโมดัลไม่เลอะข้ามไปโมดัลอื่น (ทดสอบเปิด "เพิ่มนักเรียน" ต่อจากส่งออกแล้วสล็อตว่างจริง), คอลัมน์ฐานล็อกจริงทั้งสองแบบ ติ๊กออกไม่ได้, คอลัมน์เสมือนติ๊กได้/เรนเดอร์กล่องว่างจริงไม่ใช่ `—`, `setExportColumnsAll()` เลือกทั้งหมด/ล้างค่าถูกต้อง, CSV ถอดคอลัมน์เซ็นชื่อ+สลับ layout อัตโนมัติ+ปิดตัวเลือก sheet-per-room จริง — รัน `test-multifilter.js` ซ้ำด้วย ไม่มีอะไรถดถอย (42/42) — ยังไม่ได้ดูจริงในเบราว์เซอร์ว่าแบบ B หน้าตาเทียบกับแบบ A เป็นยังไงจริง (jsdom ไม่มี CSS layout engine) รอผู้ใช้เปิดไฟล์ตรงๆ ดูแล้วเลือกว่าจะเก็บแบบไหน
